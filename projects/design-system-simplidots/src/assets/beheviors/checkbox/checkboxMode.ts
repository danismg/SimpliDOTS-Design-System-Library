export enum ConditionCheckBox {
  UNCHECK = 0,
  ISCHECK = 1,
  ISDETERMINATE = 2,
}

export enum ModeCheckBox {
  PRESSED = 0,
  ISDISABLED = 1,
  DEFAULT = 2,
}

export enum ValidCheckBox {
  DEfAULT = 0,
  INVALID = 1,
}

export enum BgCheckBox {
  ON = 0,
  OFF = 1,
  INVALID = 2,
}
